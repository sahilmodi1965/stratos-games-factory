# Stratos Factory Council

This document is the **factory's living memory**. The daemon reads it at the
start of every build run and appends it to Claude's system prompt, so any
guidance written here flows directly into every build attempt.

The weekly `council/review.sh` script (cron: Sunday 00:00 UTC) appends to
this file via Claude itself, based on what actually broke or worked in the
past 7 days. The council replaces hard-coded validation policy with a
living, data-driven feedback loop: instead of someone prescribing what
to check, the council figures out what to check based on what actually
breaks.

## Format

Each entry is one of four types:

- `### Lesson learned` — a postmortem fact derived from a real incident
- `### Known issue` — something currently broken or partly broken
- `### Architecture decision` — a design choice + the reason it was made
- `### Improvement suggestion` — a recommendation that hasn't been implemented yet

Every entry must be:
- **Concrete** — cite a real failure mode, PR number, build.log timestamp,
  or commit hash
- **Actionable** — Claude (or the next reviewer) can apply it to the next
  build
- **Time-stamped** — `(YYYY-MM-DD)` so we know how stale it is

The weekly review script appends, never rewrites. History matters.

## Council guidance for Claude (when invoked by the daemon)

When you read this file as part of your system prompt, you are not asked to
implement anything in it. You are asked to **let it shape your next build**:

1. The "Known issue" entries tell you about parts of the system that are
   currently broken or unreliable. Avoid those landmines.
2. The "Lesson learned" entries are hard-won insights from previous runs
   that went wrong. Apply them.
3. The "Architecture decision" entries explain why the system is the way
   it is. Do not undo a decision unless the issue you're working on
   explicitly asks you to revisit it.
4. The "Improvement suggestion" entries are NOT for you to act on. They
   are tracked separately by the human team.

---

# Seeded entries (2026-04-10 — initial population from the original build session)

### Lesson learned — daemon must scrub build output before commit (2026-04-10)

The first generation of the daemon ran `git add -A` after Claude finished,
which swept Vite's `docs/` build output into every PR. The "broken" PRs
#17/#18/#19 were poisoned by the daemon committing its own build output, NOT
by Claude editing forbidden paths. Fix in place: `daemon/stratos-daemon.sh`
now resets the per-game forbidden_paths field (`docs:packages:prototypes`
for arrow-puzzle, `android:capacitor.config.json:package-lock.json` for
bloxplode) to HEAD after Claude's session, before sweeping any trailing
changes.

**Council guidance for Claude**: do NOT trust `git add -A`. Use targeted
`git add <named-files>` only. The daemon's safety net will catch you if
you forget, but explicit is better.

### Lesson learned — system prompt > user message for sticky rules (2026-04-10)

The first daemon prompt put hard rules in the user message. After many tool
turns, Claude de-prioritized them and started touching forbidden paths.
Moving the rules to `--append-system-prompt` (loaded from
`templates/system-prompt-<game>.md`) made them sticky across every turn
and fixed the issue.

**Council guidance for Claude**: when you read this entry, that is your
reminder to re-check the hard rules in your system prompt before any write
operation. Especially before any `git add`.

### Lesson learned — issue premises often don't match the codebase (2026-04-10)

Issue #15 ("Speed round: 4x4 level with 30-second timer") said "Level
declares `timer: 30` in its JSON" — but Arrow Puzzle has NO per-level JSON
files. Levels are 100% procedurally generated by `level-loader.js` from
`difficulty-config.js`. The first three broken PRs (#17/#18/#19) all tried
to invent JSON-level systems that didn't exist. The PR that actually worked
(#21, after the daemon rewrite) added `SPEED_ROUNDS` to `difficulty-config.js`
in the existing pattern instead of inventing a new format.

**Council guidance for Claude**: before implementing ANY level/feature
work, verify the issue's implementation suggestion against what actually
exists. Inventing parallel systems is never correct. Match the existing
pattern or refuse with an explanation.

### Lesson learned — PR previews need relative asset paths (2026-04-10)

The PR-preview workflow deploys each PR build to `gh-pages/pr/<num>/`.
`vite.config.js` was originally configured with `base: '/arrow-puzzle-testing/'`
(absolute), so the built `index.html` referenced assets at the root. At a
preview URL like `.../arrow-puzzle-testing/pr/21/`, the browser fetched
`/arrow-puzzle-testing/assets/...` (root) instead of
`/arrow-puzzle-testing/pr/21/assets/...` (subpath) → 404 → unstyled HTML.
Fix in place: `vite.config.js` now uses `base: './'`. Both the live site
and any subpath preview now resolve correctly.

**Council guidance for Claude**: never override `base` in `vite.config.js`
without understanding both the live-site path AND the preview-path
implications. If you need to change it, document why in this file.

### Lesson learned — workflow YAML literal-block bash strings (2026-04-10)

`auto-merge.yml` and `release.yml` had a hidden YAML parse bug from day
one: bash strings inside `run: |` blocks that spanned literal newlines
without proper indentation, which made YAML think the literal block had
ended. Workflows registered with the file path as their name and never
fired their real triggers. The early "validation pass" was a false
positive — PyYAML wasn't actually installed in the env that ran the
check, and the loop's exception handler silently exited 0.

**Council guidance for Claude**: when authoring or editing GitHub Actions
YAML, validate it with a real YAML parser AND verify GitHub recognizes the
workflow name (not the file path) in `gh workflow list`. If the run name
shows the file path, the file failed parse — fix it before assuming it
works.

### Lesson learned — bash 3.2 + cron PATH on macOS (2026-04-10)

macOS ships bash 3.2 by default and cron has a minimal PATH that excludes
Homebrew, nvm, and `~/.local/bin`. Two early failures:

- `gh: command not found` at the 02:00 cron tick (fixed: `export PATH=...`
  at top of `daemon/stratos-daemon.sh`)
- `seq 0 -1` produces "0\n-1" on BSD seq instead of nothing on GNU seq
  (fixed: replaced with `while ((++i < n))` loop)

**Council guidance for Claude**: any new shell code in the daemon must be
tested under `/bin/bash` (3.2) and BSD coreutils, not GNU. Use `awk` for
text processing — it's portable. Avoid `sed -i ''` vs `sed -i` divergence
by using `awk` + tmp file instead.

### Architecture decision — structural validators only, until council recommends more (2026-04-10)

The factory currently runs `npm run validate` (paths + difficulty schema)
as its only post-Claude gate. It does NOT yet run the game in a headless
browser, take screenshots, or replay gameplay. This is deliberate: before
adding more validation infrastructure, let the council collect a few weeks
of real failure data and recommend the cheapest validators that would catch
the most failures.

Implementations explicitly **deferred** until the council recommends them
based on data:

- `window.render_game_to_text()` — agent-readable game state snapshot
- Headless puppeteer/playwright runtime QA
- Screenshot/vision QA via a second Claude session
- Action-replay-based gameplay validation
- 5-phase QA gate with bounded retries inside Claude's session

The council should look at what actually broke in the past week and
recommend which (if any) of these are worth building.

### Architecture decision — daemon prompt structure: system + compact user (2026-04-10)

Claude's session is invoked with:
- `claude -p --dangerously-skip-permissions --effort max`
- `--append-system-prompt <full content of templates/system-prompt-<game>.md
   PLUS the contents of council/COUNCIL.md>`
- A short user message containing only the issue body and a numbered to-do

The system prompt carries the immutable rules and the council's living
memory. The user message just delivers the per-issue context. This
separation is what made the rules stick across many tool turns.

**Council guidance**: do NOT inflate the user message with rules. Add new
guidance here (in COUNCIL.md) or to the per-game system prompt template,
never to the user message.

### Known issue — Pages config flip blocked on admin permission (open as of 2026-04-10)

Sahil's `sahilmodi1965` account has only `write` (not `admin`) on the
`mody-sahariar1/*` repos, so the GitHub Pages config endpoint returns 404
when the daemon tries to switch the source. Until someone with admin access
flips:

- arrow-puzzle-testing → Settings → Pages → Branch: `gh-pages` → `/ (root)`
- Bloxplode-Beta → Settings → Pages → Branch: `gh-pages` → `/ (root)`

...PR previews and live deploys are not reachable in a browser even though
the artifacts on `gh-pages` are correct. Build pipelines are unaffected.

### Improvement suggestion — open as of the council's first meeting (2026-04-10)

These are flagged for the council to evaluate during its first weekly run.
Do NOT act on them as Claude — they are tracked separately:

- Assess whether structural validators (paths + difficulty schema) catch
  enough of the real failure modes, or whether the next-cheapest gate
  (puppeteer headless runtime check) is worth adding.
- Decide whether to add `window.render_game_to_text()` to Arrow Puzzle as
  the cheapest agent-readable state snapshot.
- Decide whether the daemon should iterate Claude on validation failures
  (3-attempt cap) instead of giving up after the first run.
- Decide whether the recent-commit dedup heuristic (3+ keyword overlap)
  is producing false positives in real data.

---

# Weekly review — 2026-04-10

First weekly run. Signal is thin: exactly one successful daemon build
(issue #15 → PR #21 at 02:25-02:36), two cron ticks that bailed on auth,
and a wave of pre-rewrite PRs (#2, #3, #17, #18, #19, #20) that were all
closed without merging at the very start of the window. The factory has
not yet completed a single end-to-end "issue → merged PR → deploy" cycle
on its own. Nonetheless the week produced three concrete, data-backed
findings and one honest "no signal yet" for the deferred-validator
question.

### Known issue — dedup heuristic closed issue #16 as a false positive (2026-04-10)

The recent-commit dedup (see `recently_addressed` in
`daemon/stratos-daemon.sh:66-103`) closed **issue #16** ("Document
difficulty curriculum in Arrow_Puzzle_Game_Plan.md") with the
auto-comment *"looks like this was already addressed in commits within
the last 24 hours"* at 2026-04-09T20:56:27Z. The claim is wrong.

Evidence:
- `git grep -l "Difficulty Curriculum"` in `arrow-puzzle-testing` returns
  nothing. The section the issue asked for was never added.
- The only commit in the 24h window that touched
  `Arrow_Puzzle_Game_Plan.md` was `3f5546b` ("fix(arrow-puzzle): play
  button now switches to game screen + docs cleanup") and it changed 5
  lines of cleanup, not the curriculum section.
- Why the heuristic fired: the issue title decomposes into significant
  words `document, difficulty, curriculum, arrow, puzzle, game, plan`.
  Commit `3f5546b` contains `arrow, puzzle, play, button, switches,
  game, screen, docs, cleanup`. The overlap is `arrow + puzzle + game`
  = exactly 3 — the threshold. **The repo-name noise alone ("arrow",
  "puzzle", "game") hits the threshold** before any real semantic match
  happens.

This confirms the seeded 2026-04-10 improvement suggestion worry:
*"Decide whether the recent-commit dedup heuristic (3+ keyword overlap)
is producing false positives in real data."* Yes it is, on the very
first week of real data.

**Council guidance for Claude**: when editing the daemon's dedup logic,
do not raise the threshold (that would silence legitimate dupes). Build
a repo-specific stop-word list that strips the repo name, the game
name, and any word appearing in >=50% of recent commit subjects before
counting overlap. Filed as a council issue with the proposed fix.

### Lesson learned — 03:00:00 cron tick failed with "gh CLI not authenticated" (2026-04-10)

`build.log` shows two back-to-back `[2026-04-10 03:00:00] FATAL: gh CLI
not authenticated. Run 'gh auth login'.` lines. The 02:25:01 run 35
minutes earlier (manual, with `REPO_FILTER`/`ISSUE_FILTER` set) passed
the identical preflight (`daemon/stratos-daemon.sh:114`) and built PR
#21 to completion.

Confirmed in-session that interactive `gh auth status` still succeeds
under `sahilmodi1965 (keyring)` with `gho_…` token scopes
`gist, read:org, repo, workflow`. The failure is therefore not "token
expired" — it's environment-specific. Most likely cause on macOS: the
gh token is stored via the keyring credential helper, and cron runs in
a context where the login keychain isn't unlocked, so `gh auth status`
cannot read the token back.

This means **every hourly cron tick on Sahil's machine is at risk of
silently failing the preflight** until the auth storage is migrated or
the daemon learns to self-heal. The only reason the 02:25:01 run
succeeded is that it was a manual invocation inheriting the interactive
shell's keychain access.

**Council guidance for Claude**: do not edit the daemon preflight to
"skip on auth failure" — that would turn a loud failure into a silent
one. The right fix is diagnosing why the keychain is unreachable from
cron and either (a) moving to a file-based token, or (b) moving the
daemon to launchd. Filed as a council issue.

### Known issue — factory merge rate is 0 of 7 as of 2026-04-10 (open)

Every auto/* PR ever opened by the factory is currently closed without
merging OR still open:

- #2 (issue #1 smoke test) — closed, not merged
- #3 (manual smoke test) — closed, not merged
- #17 (issue #15 attempt 1) — closed, not merged
- #18 (issue #14 spiral) — closed, not merged
- #19 (issue #13 tutorial) — closed, not merged
- #20 (issue #12 daily challenge) — closed, not merged
- #21 (issue #15 attempt 2) — **currently open**, awaiting review

The six closed PRs were casualties of the pre-rewrite broken daemon
(documented in the seeded lessons). PR #21 is the post-rewrite success
that passed `npm run validate` and opened cleanly. It has not yet been
merged, and the Pages-config-flip known issue means even a merged PR
cannot be visually verified until admin access is granted.

Implication: **the factory has no data yet on what happens
downstream of PR creation.** Auto-merge, CI, pr-preview, deploy, and
release workflows have never been exercised on a clean daemon-authored
PR. Any recommendation about validator infrastructure should wait until
at least one full cycle completes and we see where it actually broke.

**Council guidance for Claude**: when touching the daemon, workflows,
or brain, do not declare "factory is working" based on a PR being
opened. The end-to-end success criterion is `merged=true` on an auto/*
branch with the `auto-merged` label applied, OR a human-signed merge.

### Improvement suggestion — capture per-run metrics to `council/runs.jsonl` (2026-04-10)

The weekly review script only has access to `build.log` (unstructured
text) and `gh` API queries. It cannot directly compute merge rate,
validator failure rate, average build time, or failure-mode histogram.
This is why the first weekly review has to reason from `grep` over
free-form log lines.

Proposal: have `daemon/stratos-daemon.sh` append one JSONL record per
processed issue to `council/runs.jsonl`, with fields:

```
{"ts":"2026-04-10T02:36:23Z","repo":"mody-sahariar1/arrow-puzzle-testing",
 "issue":15,"branch":"auto/...","claude_exit":0,"validate_exit":0,
 "rebase_clean":true,"changed_files":6,"pr":21,"outcome":"pr_opened"}
```

With that, the next weekly review can answer: "what % of daemon runs
produced a merged PR?", "which validators fire on which issues?",
"how long does claude take?", without re-parsing text logs. This is a
prerequisite for choosing the "cheapest validator that catches the most
failures" — we cannot rank failure modes we haven't counted. Filed as
a council issue.

### Improvement suggestion — deferred validator decisions stay deferred for at least one more week (2026-04-10)

The seeded 2026-04-10 "evaluate validators" list asks whether puppeteer
headless runtime checks, `window.render_game_to_text()`, in-session
iterative retry, etc. are worth adding. With one successful build on
the week's ledger, the honest answer is **no data yet**. PR #21 passed
`npm run validate` cleanly, so structural validators caught nothing
(good or bad) in this window. Deferring to next week's review, by
which time (a) PR #21 should be merged or reviewed, (b) metrics JSONL
(if the above proposal lands) will exist, and (c) at least one more
real build cycle should have happened.

**Council guidance for Claude**: do not pre-emptively add
puppeteer/playwright/vision-QA infrastructure this week. The seeded
architecture decision explicitly deferred them pending data, and the
data doesn't yet justify them.

---

# Weekly review — 2026-04-16

Window covers 6 days since the last review (Apr 10 → Apr 16). 34 rows in `runs.jsonl`, ~24 of them in this window. F1 was declared 2026-04-14 — most of the week was active F1 work. Strong signal: ~12 factory-improvement issues closed, the decomposition rule fired 3 times (10.3% trip rate), wayfinding stub + smoke runtime fidelity rules added in response to real incidents (#131/#139), and the inline-agent SDK pre-flight (#49) shipped. Sahil reviewed 1-1 with Ripon and confirmed: real games shipping, monetization stack mapped per game, factory direction is right.

But the same window also surfaced three structural issues worth surfacing:

### Lesson learned — schema-mandated fields don't enforce themselves (2026-04-16)

`arbitration_decision` was declared mandatory in CLAUDE.md Step 10 schema v3 on 2026-04-15. Across the 34-row history, **only 3 rows actually carry it** — all written immediately after the rule landed. The next 4 unscoped passes either omitted it entirely or filled it once and forgot. Pattern: a schema field marked "MANDATORY" in CLAUDE.md prose does not produce compliance unless the writer of `runs.jsonl` (the main thread, end of pass) has a checklist that physically lists each field and refuses to commit until present.

**Council guidance for Claude**: when adding a new schema field, also add it to the literal jq command or one-liner used to build the row. Prose-mandates without a mechanical check are wishes. Filed: factory-improvement #50.

### Lesson learned — brain regrew 14k chars in 2 days (2026-04-16)

CLAUDE.md was trimmed from 41.4k → 39.3k on 2026-04-15. By 2026-04-16 it was back to 54.3k — a regrowth of 15k chars in 2 days. Cause: each new rule added (#43 smoke fidelity, #44 wayfinding, #46 polish-PR routing, #47 council aggregation, #48 arbitration) was added BOTH as a numbered rule in the Step 3 subagent prompt template AND as a standalone prose paragraph in the surrounding Step 3 section. Each duplication is plausible in isolation; collectively, they restore exactly the bloat the previous trim removed.

**Council guidance for Claude**: when adding a rule to CLAUDE.md, pick ONE canonical place. If the rule is for the subagent → it goes in the template. If the rule is for the main thread → it goes in section prose. Never both. The template body is what the subagent reads; main-thread prose duplicating template rules is dead weight.

### Lesson learned — inline agents skip silently when their preconditions miss (2026-04-16)

Product agent fires only on `analytics-data` issues — none of the three games have any → product never fires. Monetization agent has the `#49` SDK pre-flight that skips games without ad SDKs → only fires on Bloxplode. UA agent skips games without Capacitor → fires on no game. Result: three of five inline agents have produced near-zero F1-relevant output across the entire factory lifetime. They become useful only AFTER the game crosses G2 — which means they cannot HELP a game cross G2.

This is the gap factory-improvement #30 has been tracking unfunded for 3+ days. The fix is the G1 strategy mode being shipped this pass: every inline agent fires from G1 onward in advisory mode, files prerequisite issues for its own data mode, and contributes a strategy doc even before data exists. Documented in this pass's brain edit; #30 closed.

### Architecture decision — Sahil-as-floodgates is the design, not a bottleneck (2026-04-16)

Sahil has confirmed in 2026-04-16 1-1 notes that he is intentionally the PR-review choke point until the factory has proven itself end-to-end on F1. Factory-improvement #23 (auto-merge trust ladder) is therefore **not urgent** and stays F2-tagged. The factory should respect Sahil's review pace: when the auto-PR backlog reaches ~3 open PRs across the portfolio, **stop generating new game work** until they drain. The factory's job in those windows is brain-fix, factory-improvement closure, and council/observation-routing review — not more game PRs.

This decision binds Step 1 assess: include "auto-PR backlog ≥ 3" as a signal that this pass should be brain-focused, not game-focused.

### Architecture decision — inline agents fire from G1 in two modes (2026-04-16)

Each of product / monetization / UA agents now operates in two modes:

- **Strategy mode** (G1+): no upstream data required. Files (a) the prerequisite integration issue (`[G2] feat: integrate Firebase Analytics` etc.) and (b) one strategy advisory issue per game per ~30 days documenting the agent's plan for when data lands.
- **Data mode** (G2+): the existing behavior — pulls real data, files data-backed optimization issues.

Each agent picks the mode based on whether its data source exists. Strategy-mode artifacts build the knowledge base BEFORE the game ships, so when data arrives the agent already knows where to deploy it. Encoded in CLAUDE.md Steps 4/5/8 in this pass's brain edit. Closes factory-improvement #30 (inactive agent audit).

### Improvement suggestion — secure key-sharing pattern for Ripon (2026-04-16)

Ripon will execute store submissions, ad SDK setup, MMP integrations — all of which require API keys, signing certs, store credentials. The factory has zero defined mechanism for sharing these securely. Sahil's 2026-04-16 directive #5 surfaces this as an F1 blocker for any game that needs to actually cross G2. Filed: factory-improvement #51.

### Improvement suggestion — runs.jsonl row-builder helper (2026-04-16)

Today the runs.jsonl row is hand-written at the end of every pass. The mandatory-field problem above (arbitration_decision missing on 91% of rows) is a symptom — there's no helper that prompts for each schema field. Proposal: a `scripts/log-run.sh` that takes named args and emits a schema-compliant JSON row, refusing to write if any required field is absent. Folded into factory-improvement #50.

### Decomposition rule trips — last 5 (2026-04-16)

3 trips over 29 shipped PRs in the 50-row window = **10.3%, bottom edge of healthy band (10-30%)**. Trips:

- arrow-puzzle #136 → #137 + #138 — both shipped, smoke green
- arrow-puzzle #88 → structure + polish — both shipped
- (one earlier trip prior to schema v3, not detailed)

Healthy. Sample of 10 most recent build-request issue bodies (not split by the rule) suggests issue-writing is pre-splitting cleanly rather than the heuristic missing real splits. No action.

### Brain-vs-game ratio — directional only (2026-04-16)

Of the 3 rows that carry `arbitration_decision`: 1 brain, 1 game, 1 mixed. With "mixed" counted as half-brain, that's 50% brain — above the healthy band. But 3 rows is too thin to act on; until ~15 rows carry the field, treat as directional only. Factory-improvement #50 (mandatory-field enforcement) is the prerequisite for this aggregation to become decisive.

### `factory_delta` rollup — last 7 days (2026-04-16)

Filed factory-improvements: 9 (#34-49 range). Closed: ~12. Memory writes: 8. Brain edits: 9. Net: factory IS contributing back to itself, healthily. The rule "if factory_delta is empty across the window, sessions are consuming without paying back" is satisfied — every working day this week added at least one tracked artifact.

---

End of 2026-04-16 review. Next review window: 2026-04-23.

---

# Correction entries — 2026-04-17

### Architecture decision — review-gate ownership is Ripon's, not Sahil's (2026-04-17, supersedes 2026-04-16 "Sahil-as-floodgates")

Sahil clarified on 2026-04-17 that the 2026-04-16 "Sahil-as-floodgates" entry misread his role. **Sahil does NOT review or merge any auto/\* PR** — that gate is entirely Ripon's. Sahil's role is strategist / orchestrator / brain-keeper: he runs `go`, edits CLAUDE.md + `council/*`, closes factory-improvement issues, makes milestone + architecture calls, and takes ad-hoc tasks when specifically needed.

**Operational consequences:**

1. The PR-backlog pause rule still applies (≥3 open auto-PRs → swarm pauses new game work), but the bottleneck is explicitly **Ripon's review pace**, not Sahil's. This is not a constraint to engineer around; it's the quality gate Ripon personally plays-tests before merging.
2. Sahil's concurrent work (brain-fix, factory-improvement closure, council review, ad-hoc) is **not gated** by the PR backlog. He can ship factory-improvements while Ripon's review queue drains.
3. Auto-merge trust ladder (factory-improvement #23) remains F2-deferred — **because Ripon wants manual review during F1 prove-out**, not because Sahil is the bottleneck. The bottleneck framing was wrong.
4. The brain + memory are updated to reflect this: new `CLAUDE.md` Roles section (Sahil / Ripon / Factory), updated `user_sahil.md` memory, new `user_ripon.md` memory.

**Why the earlier entry was wrong:** the 2026-04-16 entry inferred Sahil's role from his earlier "hands-off" description, then conflated that with the PR-review gate. Both inferences were mine, not Sahil's. The actual division is: Sahil = strategy + brain; Ripon = ops including PR merge. The `factory_delta` audit didn't catch this because role-ownership wasn't a tracked schema field.

### Lesson learned — don't infer role boundaries from indirect signals (2026-04-17)

The council entry naming the wrong person as review-gate owner cost no production time (Ripon was already reviewing in practice), but it did cost one incorrect action-plan table handed to Sahil before he corrected the model. Future lesson: when the brain writes an architecture decision about **who does what**, verify with the relevant person before committing the entry. Role boundaries aren't inferrable from workflow evidence alone — confirm them explicitly.

