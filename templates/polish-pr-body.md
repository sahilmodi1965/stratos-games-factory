Closes #<ISSUE>

# 🎨 Visual polish PR

**Preview:** <PREVIEW_URL>

This is a `[polish]` PR per the decomposition rule (factory CLAUDE.md Step 3). All visual knobs live in a single CSS variables file (`<TUNABLES_FILE>`) so iteration is a 1-line edit, not a rebuild. The mechanical structure this polish sits on top of is already merged to main — **nothing structural is at risk in this PR.**

## How to respond

Open the preview on your phone. Pick one:

- 👍 **Ship it** — react with 👍 and Sahil merges.
- 🎯 **Tweak the knobs** — reply with exact CSS variable changes. Examples:
  ```
  --tutorial-hand-rotation: 150deg
  --tutorial-hand-width: 110px
  --tutorial-hand-label-offset: 32px
  ```
  Each reply ships as a new 5-line commit on the same PR — not a new PR, not a new issue.
- 📸 **Visual feedback** — attach a screenshot with arrows/scribbles showing what's wrong. The swarm will translate that into the right variable values on the next go.
- ❌ **Reject** — react with 👎 and comment with what you actually want. A follow-up polish PR will iterate.

## ⚠ Do NOT close this PR

Closing this PR throws away the structural work underneath and forces the factory to rebuild from scratch. **That is the antipattern that killed tutorial PRs #75, #80, and #85** — and the reason the decomposition rule exists. If something is fundamentally wrong, react with 👎 and comment with what you want instead. Do not close.

## Tunable variables in this PR

All visual knobs are defined in `<TUNABLES_FILE>`. Edit that file to iterate — no source-code changes needed for visual tweaks.

---
Generated by the Stratos Games Factory swarm.
